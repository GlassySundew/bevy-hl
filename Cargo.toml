[package]
name = "bevy-hashlink-native"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["staticlib"]
path = "rust/src/lib.rs"

[dependencies]
bevy_ecs = { version = "=0.19.0", default-features = false }

[profile.release]
codegen-units = 1
lto = "thin"
panic = "abort"
